{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python -m venv venv && . venv/bin/activate && pip install -r backend-requirements.txt"
  },
  "deploy": {
    "startCommand": ". venv/bin/activate && uvicorn api:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}